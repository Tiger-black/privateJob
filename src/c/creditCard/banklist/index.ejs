
<% data.forEach(function(item){ 
	if(item.id != '-1'){ %>
	<div class="bankItem">
		<% if(item.searchLogoUrl){ %>
			<img src="<%= item.searchLogoUrl.replace(/(.*)\./, '$1_60x60q90.')%>" alt="">
		<% }else if(item.logoUrl){ %>
			<img src="<%= item.logoUrl.replace(/(.*)\./, '$1_60x60q90.')%>" alt="">
		<% } %>
		<p><%= item.name%></p>
	</div>
<% }}) %>